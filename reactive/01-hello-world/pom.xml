<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.flowwarden.examples</groupId>
        <artifactId>flowwarden-examples-reactive</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>flowwarden-example-reactive-01-hello-world</artifactId>
    <name>FlowWarden Example — Reactive 01 Hello World</name>
    <description>
        Minimal end-to-end wiring in reactive mode: @EnableFlowWarden +
        @ChangeStream + @OnInsert returning Mono&lt;Void&gt;.
    </description>

    <dependencies>
        <dependency>
            <groupId>io.flowwarden.examples</groupId>
            <artifactId>examples-common</artifactId>
        </dependency>

        <!--
            Both Mongo starters are required: the reactive starter drives
            the change stream and the data generator, and the imperative
            starter satisfies a sync MongoDatabaseFactory that
            FlowWarden's reactive auto-configuration depends on internally
            (1.0.0-rc.1).
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb-reactive</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.flowwarden</groupId>
            <artifactId>flowwarden-stream-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mongodb</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
